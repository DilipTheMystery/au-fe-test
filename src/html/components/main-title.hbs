{{!-- It is the main project info container. The componet get data from json via assemble --}}
<div class="row main-title">
  <div class="col-md-12 text-center">
    <h1 class="title text-uppercase">{{title}}</h1>
    <div class="subline">{{{subline}}}</div>
    <div class="description">
      {{description}}
    </div>
    <a class="visit-btn" href="{{visitUrl}}">{{visitLabel}}</a>
  </div>
</div>